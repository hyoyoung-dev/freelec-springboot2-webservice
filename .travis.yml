language: java
jdk:
  - openjdk11

branches:
  only:
    - master

# Travis CI Home
cache:
  directories:
    - '$HOME/.m2/repository'
    - '$HOME/.gradle'

script: "./gradlew clean build"

# Send e-mail when CI is completed
notifications:
  email:
    recipients:
      - hyshin0@gmail.com

